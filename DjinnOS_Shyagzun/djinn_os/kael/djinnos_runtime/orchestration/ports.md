# Ports (Kael runtime)

Kernel API:
- internal: 8080
- host: configurable via KERNEL_PORT
- rule: only networking changes are allowed; semantics must remain identical.

No other ports are normative in v0.1.1.
